# Copier en .env sur le VPS (à côté de compose.yaml).
IMAGE=ghcr.io/<github-user>/<repository>/mcp-paie-fr
IMAGE_TAG=latest
MCP_DOMAIN=mcp-paie.example.com
HOST_PORT=3000
RATE_LIMIT_PER_MINUTE=600
LOG_LEVEL=info
